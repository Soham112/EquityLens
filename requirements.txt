# Core data
yfinance>=0.2.40        # price, ATR, RSI, technicals, FCF, revenue growth
pandas>=2.0.0
numpy>=1.26.0

# Chart rendering (chart vision agent)
matplotlib>=3.8.0
mplfinance>=0.12.10b0

# API framework + dashboard
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# HTTP client
requests>=2.31.0

# Storage (optional — set SUPABASE_URL env var to activate)
supabase>=2.4.0

# Utilities
python-dotenv>=1.0.0

# Data sources (via MCP — no pip install needed):
#   Bigdata.com   → fundamentals, sentiment, analyst ratings, earnings
#   SEC EDGAR     → free filing fallback
